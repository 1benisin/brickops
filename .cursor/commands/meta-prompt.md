---
allowed-tools: Write, Edit, WebFetch, Task, mcp_firecrawl-mcp_firecrawl_scrape, Fetch description: Create a new prompt based on a user's request
---

# Purpose

This meta prompt takes the `USER_PROMPT_REQUEST` and follows the `Workflow` to create a new prompt in the `Specified Format`.

## Variables

## Instructions

## Workflow

## Specified Format
