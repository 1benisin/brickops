import { mutationGeneric, queryGeneric } from "convex/server";

// Temporary stubs so Convex functions compile before running `npx convex dev`.
export const mutation = mutationGeneric;
export const query = queryGeneric;
